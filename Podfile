# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'NYTimes_MPA' do
  # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!

    # Pods for NYTimes

    target 'NYTimes_MPATests' do
      inherit! :search_paths
      # Pods for testing

    end

    target 'NYTimes_MPAUITests' do
      inherit! :search_paths
      # Pods for testing
    end

      pod 'SwiftLint', :inhibit_warnings => true
      pod 'Alamofire', '~> 4.7' , :inhibit_warnings => true
      pod 'ARSLineProgress', '~> 2.0' , :inhibit_warnings => true
      pod 'Kingfisher', '~> 4.0' , :inhibit_warnings => true
  end
